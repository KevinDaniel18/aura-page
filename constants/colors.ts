const colors = {
  primary: "#4CAF50", // Verde (para el color principal)
  background: "#D7CCC8", // Café claro (para el fondo)
  secondary: "#6D4C41", // Café oscuro (para el color secundario)
  textPrimary: "#333333", // Texto principal (sin cambios)
  textSecondary: "#666666", // Texto secundario (sin cambios)
  accent: "#81C784", // Verde suave (para acentos)
};

export const {
  primary,
  background,
  secondary,
  textPrimary,
  textSecondary,
  accent,
} = colors;
